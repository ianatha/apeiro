use crate::apeiro_bundle_and_compile;

// #[tokio::test(flavor = "multi_thread", worker_threads = 2)]
// async fn test_bundle() {
//     let out = apeiro_bundle_and_compile(include_str!("fn_bundle.main.ts").into()).unwrap();
//     println!("{}", out.source_map.unwrap_or("".into()));
//     println!("{}", out.compiled_src);
// }
